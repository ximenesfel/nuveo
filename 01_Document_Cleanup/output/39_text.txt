There are several classic spatial filters for reducing or elimin
from images. The mean filter, the median filter and the closing o
used. The snean filter is a towpass or smoothing filter that replace
neighborhood mean. ft reduces the image noise but blurs the imag
calculates the median of the pixel neighborhood f.r each pixel, th:
effect, Finally, the opening closing filter is a mathew -tical morph
the same number of erosion and dilation morpholo-.icul operations
objects from"images. °
. The main goal was to train a neural netwoi! in « supervised
image frei a noisy one. In this particular case, it yas nvich easier
_ image from a clean one than to clean a subset ¢! n isy images.
